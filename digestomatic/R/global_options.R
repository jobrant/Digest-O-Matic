# project_path <- '/srv/shiny-server/DIGEST/'

# options(shiny.maxRequestSize = 1000*4096^2)

# expTblOptions <- list(paging = T,
#                       dom = 'ft',
#                       scrollX = T,
#                       fixedColumns = T,
#                       scroller = T,
#                       scrollY = 365,
#                       rownames = F,
#                       order = list(3, 'asc'),
#                       columnDefs = list(list(
#                         className = 'dt-center',
#                         targets = '_all')))





